import "jsr:@supabase/functions-js/edge-runtime.d.ts";

const U=Deno.env.get("SUPABASE_URL")!,K=Deno.env.get("SUPABASE_SERVICE_ROLE_KEY")!,COOKIE="sb_market_review_session";
const esc=(v:any)=>String(v??"").replace(/[&<>"']/g,m=>({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;","'":"&#39;"}[m]||m));
const cop=(v:any)=>v==null?"—":new Intl.NumberFormat("es-CO",{style:"currency",currency:"COP",maximumFractionDigits:0}).format(Number(v));
const dt=(v:any)=>v?new Intl.DateTimeFormat("es-CO",{timeZone:"America/Bogota",dateStyle:"short",timeStyle:"short"}).format(new Date(v)):"—";
const css=`:root{--bg:#f4f6f3;--p:#fff;--t:#182018;--m:#687168;--l:#dfe5de;--g:#123d2a;--g2:#1f6a49;--a:#765400;--r:#842828}*{box-sizing:border-box}body{margin:0;background:var(--bg);color:var(--t);font:14px/1.45 Inter,system-ui,sans-serif}a{text-decoration:none;color:inherit}.top{background:var(--g);color:#fff;padding:18px 26px;display:flex;justify-content:space-between;gap:14px;align-items:center}.top h1{margin:2px 0}.ey{font-size:10px;letter-spacing:1.4px;font-weight:800;opacity:.72}main{padding:20px 26px 45px;max-width:1600px;margin:auto}.nav,.filters,.actions{display:flex;gap:7px;flex-wrap:wrap;align-items:center}.btn,button,select,input,textarea{border:1px solid var(--l);border-radius:8px;padding:8px 10px;background:#fff;font:inherit}.btn,button{font-weight:750;cursor:pointer}.btn.primary,button.primary{background:var(--g2);border-color:var(--g2);color:#fff}.notice{padding:12px 14px;border:1px solid #ead99c;background:#fff3cf;color:#65490d;border-radius:10px;margin-bottom:14px}.ok{background:#e1f2e6;border-color:#b8dbc3;color:#175935}.panel,.metric{background:var(--p);border:1px solid var(--l);border-radius:13px}.metrics{display:grid;grid-template-columns:repeat(5,1fr);gap:10px;margin-bottom:14px}.metric{padding:14px}.metric span,.sub,label span{display:block;color:var(--m);font-size:11px}.metric strong{font-size:22px}.head{padding:15px 17px;border-bottom:1px solid var(--l);display:flex;justify-content:space-between;gap:12px;align-items:center}.head h2{margin:0}.tablewrap{overflow:auto}table{border-collapse:collapse;width:100%;min-width:1250px}th,td{padding:9px 10px;border-bottom:1px solid #edf0ec;text-align:left;white-space:nowrap;vertical-align:top}th{font-size:10px;text-transform:uppercase;color:var(--m);background:#fafbf9}.vehicle{white-space:normal;min-width:300px}.pill{font-size:10px;font-weight:800;border-radius:99px;padding:5px 8px;display:inline-block}.ready{background:#daf0e1;color:#175935}.blocked{background:#f4e5e5;color:var(--r)}.manual{background:#e8eef3;color:#425c70}.none{background:#ecefec;color:#5c665d}.section{padding:16px}.grid{display:grid;grid-template-columns:repeat(4,1fr);gap:10px}.kv{background:#f8faf7;border-radius:9px;padding:10px}.kv span{display:block;font-size:10px;color:var(--m);text-transform:uppercase}.formgrid{display:grid;grid-template-columns:2fr 1fr;gap:12px}.formgrid textarea{min-height:210px;width:100%;font-family:ui-monospace,SFMono-Regular,Menlo,monospace}.formgrid input{width:100%}.confirm{display:flex;gap:8px;align-items:center;margin:12px 0}.confirm input{width:auto}.login{width:min(430px,92vw);margin:15vh auto;background:#fff;border:1px solid var(--l);border-radius:16px;padding:28px}.login input{width:100%}.login button{width:100%;margin-top:10px}.err{color:var(--r);font-weight:700}.history{margin-top:14px}.history a{color:var(--g2);text-decoration:underline}@media(max-width:900px){.metrics{grid-template-columns:repeat(2,1fr)}.grid,.formgrid{grid-template-columns:1fr}.top{align-items:flex-start}main{padding:13px}}`;
function html(title:string,body:string,status=200,headers:Record<string,string>={}){return new Response(`<!doctype html><html lang="es"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>${esc(title)}</title><style>${css}</style></head><body>${body}</body></html>`,{status,headers:{"content-type":"text/html; charset=utf-8","cache-control":"no-store",...headers}})}
async function db(path:string,init:RequestInit={}){return fetch(`${U}${path}`,{...init,headers:{apikey:K,authorization:`Bearer ${K}`,"content-type":"application/json",...(init.headers||{})}})}
function cookie(req:Request){for(const p of (req.headers.get("cookie")||"").split(";")){const [k,...v]=p.trim().split("=");if(k===COOKIE)return decodeURIComponent(v.join("="))}return ""}
async function valid(t:string){if(!t||t.length<32||t.length>256)return false;const r=await db("/rest/v1/rpc/dashboard_token_valid",{method:"POST",body:JSON.stringify({p_token:t})});return r.ok&&(await r.json())===true}
function pathOf(req:Request){const p=new URL(req.url).pathname,m="/superbid-market-review-dashboard",i=p.indexOf(m);return i>=0?(p.slice(i+m.length)||"/"):p}
function login(error=false){return html("SUPERBID — Revisión mercado",`<form class="login" method="post" action="/functions/v1/superbid-market-review-dashboard/login"><div class="ey">SUPERBID · v0.44</div><h1>Revisión de mercado</h1><p class="sub">Acceso privado para registrar evidencia real de comparables. No es una señal de compra.</p>${error?'<p class="err">Credencial inválida.</p>':''}<input type="password" name="password" autocomplete="current-password" required placeholder="Clave de acceso"><button class="primary">Entrar</button></form>`,error?401:200)}
const nav=`<nav class="nav"><a class="btn" href="/functions/v1/superbid-dashboard">Dashboard</a><a class="btn" href="/functions/v1/superbid-readiness-dashboard">Readiness</a><a class="btn" href="/functions/v1/superbid-fasecolda-workbench">Fasecolda</a><form method="post" action="/functions/v1/superbid-market-review-dashboard/logout"><button>Salir</button></form></nav>`;
function status(v:any){const x=String(v||"PENDING"),c=x==="READY"?"ready":x==="MANUAL_REVIEWED"?"manual":x==="AUTH_REQUIRED"?"blocked":"none";return `<span class="pill ${c}">${esc(x)}</span>`}
async function rows(){const f="external_lot_id,lot_id,title,city,seller,current_bid_cop,closes_at,hours_to_close,review_state,review_score,readiness_status,next_action,blocker_count,blockers,fasecolda_status,effective_market_status,market_evidence_origin,market_comparable_count,p25_asking_cop,quick_sale_cop,market_confidence,market_observed_at,manual_evidence_set_id,manual_comparable_count,manual_evidence_fingerprint,manual_reviewed_at,interpretation";const r=await db(`/rest/v1/dashboard_market_review_queue_v44?select=${encodeURIComponent(f)}&order=blocker_count.asc,review_score.desc,closes_at.asc&limit=350`);if(!r.ok)throw new Error(`market queue ${r.status}`);return r.json()}
async function board(){const xs=await rows();const ready=xs.filter((x:any)=>x.effective_market_status==="READY").length,manual=xs.filter((x:any)=>x.market_evidence_origin==="MANUAL_REVIEWED").length,fase=xs.filter((x:any)=>x.fasecolda_status==="HIGH").length;const cards=[["Pendientes mercado",xs.length],["Mercado READY",ready],["Manual revisado",manual],["Fasecolda HIGH",fase],["OAuth Mercado Libre","APP_REQUIRED"]].map(x=>`<div class="metric"><span>${esc(x[0])}</span><strong>${esc(x[1])}</strong></div>`).join("");const body=xs.map((x:any)=>`<tr><td class="vehicle"><strong>${esc(x.title)}</strong><div class="sub">Lote ${esc(x.external_lot_id)} · ${esc([x.city,x.seller].filter(Boolean).join(" · "))}</div></td><td>${esc(x.fasecolda_status||"—")}</td><td>${status(x.effective_market_status)}<div class="sub">${esc(x.market_evidence_origin||"sin evidencia")}</div></td><td>${esc(x.market_comparable_count||0)}</td><td>${cop(x.p25_asking_cop)}</td><td>${cop(x.quick_sale_cop)}</td><td>${x.market_confidence==null?"—":`${Math.round(Number(x.market_confidence)*100)}%`}</td><td>${esc(x.blocker_count)}</td><td>${esc(x.next_action)}</td><td>${dt(x.closes_at)}</td><td><a class="btn primary" href="/functions/v1/superbid-market-review-dashboard/lots/${esc(x.external_lot_id)}">Revisar mercado</a></td></tr>`).join("");return html("SUPERBID — Mercado",`<header class="top"><div><div class="ey">SUPERBID · v0.44</div><h1>Auditable Market Evidence</h1></div>${nav}</header><main><div class="notice"><strong>MANUAL_MARKET_EVIDENCE_NOT_AUTOMATIC_VALUATION · MARKET_REVIEW_NOT_BUY_SIGNAL.</strong> Esta interfaz registra evidencia humana auditable. Un set REVIEWED puede satisfacer la capa de mercado, pero no modifica Fasecolda, costos, comisión, puja actual ni fuerza COMPRAR.</div><section class="metrics">${cards}</section><section class="panel"><div class="head"><div><h2>${xs.length} lotes con mercado pendiente</h2><span class="sub">Prioridad: menos blockers, mayor review score y cierre más próximo.</span></div></div><div class="tablewrap"><table><thead><tr><th>Vehículo</th><th>Fasecolda</th><th>Mercado</th><th>Comps</th><th>P25</th><th>Quick sale</th><th>Conf.</th><th>Blockers</th><th>Siguiente</th><th>Cierre</th><th>Acción</th></tr></thead><tbody>${body||'<tr><td colspan="11">Sin casos pendientes.</td></tr>'}</tbody></table></div></section></main>`)}
async function lot(external:string,saved=false,error=""){const xs=await rows(),x=xs.find((v:any)=>String(v.external_lot_id)===external);if(!x)return html("No encontrado","<main>Lote no encontrado en la cola de mercado.</main>",404);const setsR=await db(`/rest/v1/market_manual_evidence_sets?select=id,status,source_note,comparable_count,median_asking_cop,p25_asking_cop,p75_asking_cop,quick_sale_cop,dispersion_pct,confidence,evidence_fingerprint,reviewed_at,created_at&lot_id=eq.${encodeURIComponent(x.lot_id)}&order=created_at.desc&limit=10`);const sets=setsR.ok?await setsR.json():[];const hist=sets.map((s:any)=>`<tr><td>${esc(s.id)}</td><td>${status(s.status)}</td><td>${esc(s.comparable_count)}</td><td>${cop(s.p25_asking_cop)}</td><td>${cop(s.quick_sale_cop)}</td><td>${s.confidence==null?"—":`${Math.round(Number(s.confidence)*100)}%`}</td><td>${dt(s.reviewed_at||s.created_at)}</td><td class="vehicle">${esc(s.source_note||"—")}</td></tr>`).join("");return html(`SUPERBID — ${external}`,`<header class="top"><div><div class="ey">SUPERBID · mercado v0.44</div><h1>${esc(x.title)}</h1></div>${nav}</header><main>${saved?'<div class="notice ok">Evidencia guardada. El readiness se recalcula desde la vista central; guardar evidencia no equivale a comprar.</div>':''}${error?`<div class="notice"><strong>Error:</strong> ${esc(error)}</div>`:''}<div class="notice"><strong>MARKET_REVIEW_NOT_BUY_SIGNAL.</strong> Para marcar REVIEWED se requieren al menos 3 comparables reales, HTTPS, únicos y del mismo año del lote. Usa una línea por comparable: <code>URL | PRECIO_COP | AÑO | TÍTULO | CIUDAD</code>.</div><section class="panel"><div class="head"><h2>Estado del lote ${esc(external)}</h2><a class="btn" href="/functions/v1/superbid-market-review-dashboard">Volver</a></div><div class="grid"><div class="kv"><span>Fasecolda</span><strong>${esc(x.fasecolda_status||"—")}</strong></div><div class="kv"><span>Mercado efectivo</span><strong>${esc(x.effective_market_status||"—")}</strong><small class="sub">${esc(x.market_evidence_origin||"sin evidencia")}</small></div><div class="kv"><span>Puja actual</span><strong>${cop(x.current_bid_cop)}</strong></div><div class="kv"><span>Readiness</span><strong>${esc(x.readiness_status)}</strong><small class="sub">${esc(x.next_action)}</small></div></div><div class="section"><h3>Nueva evidencia</h3><form method="post" action="/functions/v1/superbid-market-review-dashboard/lots/${esc(external)}/review"><div class="formgrid"><label><span>Comparables</span><textarea name="comparables" required placeholder="https://ejemplo.com/vehiculo-1 | 42000000 | 2020 | Renault Duster Intens 2020 | Medellín&#10;https://ejemplo.com/vehiculo-2 | 43500000 | 2020 | Renault Duster Zen 2020 | Bogotá&#10;https://ejemplo.com/vehiculo-3 | 41000000 | 2020 | Renault Duster 2020 | Cali"></textarea></label><div><label><span>Nota de fuente/revisión</span><textarea name="source_note" placeholder="Dónde se buscaron los comparables y qué criterio humano se usó."></textarea></label><label class="confirm"><input type="checkbox" name="mark_reviewed" value="1">Marcar como REVIEWED</label><p class="sub">Sin marcar se guarda DRAFT. REVIEWED requiere ≥3 líneas y nota de al menos 10 caracteres.</p><button class="primary">Guardar evidencia</button></div></div></form><div class="history"><h3>Histórico de sets</h3><div class="tablewrap"><table><thead><tr><th>Set</th><th>Estado</th><th>Comps</th><th>P25</th><th>Quick sale</th><th>Conf.</th><th>Fecha</th><th>Nota</th></tr></thead><tbody>${hist||'<tr><td colspan="8">Sin evidencia manual registrada.</td></tr>'}</tbody></table></div></div></div></section></main>`)}
function parseComparableLines(raw:string){const lines=raw.split(/\r?\n/).map(x=>x.trim()).filter(Boolean);if(lines.length<1||lines.length>20)throw new Error("Debes ingresar entre 1 y 20 comparables.");return lines.map((line,i)=>{const parts=line.split("|").map(x=>x.trim());if(parts.length<4)throw new Error(`Línea ${i+1}: usa URL | PRECIO_COP | AÑO | TÍTULO | CIUDAD.`);const [url,price,year,title,city=""]=parts;if(!/^https:\/\/\S+$/.test(url))throw new Error(`Línea ${i+1}: URL HTTPS inválida.`);if(!/^\d{6,10}$/.test(price))throw new Error(`Línea ${i+1}: precio COP inválido.`);if(!/^\d{4}$/.test(year))throw new Error(`Línea ${i+1}: año inválido.`);if(title.length<3)throw new Error(`Línea ${i+1}: título requerido.`);return {url,asking_price_cop:Number(price),model_year:Number(year),title,city,observed_at:new Date().toISOString()}})}
async function saveReview(req:Request,external:string){const f=await req.formData(),raw=String(f.get("comparables")||""),note=String(f.get("source_note")||""),reviewed=f.get("mark_reviewed")==="1";try{const comps=parseComparableLines(raw);if(reviewed&&comps.length<3)throw new Error("REVIEWED requiere al menos 3 comparables.");const r=await db("/rest/v1/rpc/dashboard_save_manual_market_evidence",{method:"POST",body:JSON.stringify({p_external_lot_id:external,p_comparables:comps,p_source_note:note,p_mark_reviewed:reviewed})});if(!r.ok){let msg=`RPC ${r.status}`;try{const j=await r.json();msg=j.message||j.error||msg}catch{}throw new Error(msg)}return new Response("",{status:303,headers:{location:`/functions/v1/superbid-market-review-dashboard/lots/${external}?saved=1`}})}catch(e){return lot(external,false,e instanceof Error?e.message:"No fue posible guardar la evidencia.")}}
Deno.serve(async req=>{const p=pathOf(req);if(req.method==="POST"&&p==="/login"){const f=await req.formData(),pw=String(f.get("password")||"");if(!await valid(pw))return login(true);return new Response("",{status:303,headers:{location:"/functions/v1/superbid-market-review-dashboard","set-cookie":`${COOKIE}=${encodeURIComponent(pw)}; Path=/functions/v1/superbid-market-review-dashboard; HttpOnly; Secure; SameSite=Strict; Max-Age=604800`}})}if(req.method==="POST"&&p==="/logout")return new Response("",{status:303,headers:{location:"/functions/v1/superbid-market-review-dashboard","set-cookie":`${COOKIE}=; Path=/functions/v1/superbid-market-review-dashboard; HttpOnly; Secure; SameSite=Strict; Max-Age=0`}});if(!await valid(cookie(req)))return login(false);if(req.method==="GET"&&(p==="/"||p===""))return board();const m=p.match(/^\/lots\/(\d{5,12})$/);if(req.method==="GET"&&m)return lot(m[1],new URL(req.url).searchParams.get("saved")==="1");const s=p.match(/^\/lots\/(\d{5,12})\/review$/);if(req.method==="POST"&&s)return saveReview(req,s[1]);return html("No encontrado","<main>Ruta no encontrada.</main>",404)});
